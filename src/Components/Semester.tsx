import { ReactElement, useState } from "react";
/*
type Props = {
    children: ReactElement[];
 */

 
/* You will probally need to make another useStates(1) for the prevs button 
 const Tabs: React.FC<Props> = ({ children }) => {
     const [ tables, setTable ] = useState(0);
  
     return (
         <div>
             <ul>
                 {tables.map(() => 
                 <Tables/>
                 )}
             </ul>
             {children[tables]}
         </div>
     );
 };
*/ 